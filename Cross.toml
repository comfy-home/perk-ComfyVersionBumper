[target.aarch64-unknown-linux-gnu]
image = "ghcr.io/cross-rs/aarch64-unknown-linux-gnu:main"

#[build]
#xargo = false
# Isolate cross-build artifacts so host-compiled build scripts (glibc 2.43)
# don't bleed into the container (glibc 2.31) and cause version mismatch errors.
#target-dir = "/target"
